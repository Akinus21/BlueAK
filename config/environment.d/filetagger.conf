# FileTagger environment configuration
# This file is read by systemd user services via EnvironmentFile=
# Edit this file to point filetagger at your Ollama instance.
#
# After editing, reload with:
#   systemctl --user daemon-reload
#   systemctl --user restart filetagger

FILETAGGER_OLLAMA_URL=https://ollama.akinus21.com
FILETAGGER_OLLAMA_MODEL=gpt-oss:20b-cloud
# FILETAGGER_WATCH_DIR=%h/files   # uncomment to override default
